apiVersion: 1
providers:
  - name: '4g/5g box'
    folder: '' # Optional: set a specific folder name, or '' for General
    type: file
    disableDeletion: true # Prevents accidental deletion via the UI
    editable: true # Allows editing in the UI (changes won't persist on restart)
    options:
      path: /etc/grafana/provisioning/dashboards/files